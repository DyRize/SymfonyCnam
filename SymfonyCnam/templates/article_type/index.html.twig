{% extends 'base.html.twig' %}

{% block title %}ArticleType index{% endblock %}

{% block body %}
    <h1>ArticleType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>#</th>
                <th>Code</th>
                <th>Libellé</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
        {% for article_type in article_types %}
            <tr>
                <td>{{ article_type.id }}</td>
                <td>{{ article_type.code }}</td>
                <td>{{ article_type.label }}</td>
                <td>
                    <a href="{{ path('article_type_show', {'id': article_type.id}) }}">show</a>
                    <a href="{{ path('article_type_edit', {'id': article_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('article_type_new') }}">Create new</a>
{% endblock %}
