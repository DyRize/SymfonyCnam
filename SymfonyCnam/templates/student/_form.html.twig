{{ form_start(form) }}
	{{ form_row(form.arrivedAt) }}
	{{ form_row(form.graduatedAt) }}
	{{ form_row(form.relatedUser) }}
	{{ form_row(form.promotion) }}
	{{ form_row(form.subjects) }}
	{{ form_row(form.projects) }}
    <button class="btn_common btn_border margin10 border_radius">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
