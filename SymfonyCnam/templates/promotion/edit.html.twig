{% extends 'base.html.twig' %}

{% block title %}Edition de la promotion {{ promotion.code }}{% endblock %}

{% block body %}
	<h1>Edition de la promotion <strong>{{ promotion.code }}</strong></h1>
	<a href="#promotion" class="btn btn-danger">Allons y !</a>
{% endblock %}

{% block section %}
	<section id="promotion">
		<div class="row">
			<div class="col-sm-12">
				<div class="card">
					<div class="card-body">
						<div class="row">
							{{ include('promotion/_form.html.twig', {'button_label': 'Update'}) }}

							<a href="{{ path('promotion_index') }}">back to list</a>

							{{ include('promotion/_delete_form.html.twig') }}
						</div>
					</div>
				</div>
			</div>
		</div>
	</section>
{% endblock %}
