{% extends 'base.html.twig' %}

{% block title %}TeacherType{% endblock %}

{% block body %}
    <h1>TeacherType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ teacher_type.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ teacher_type.code }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ teacher_type.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('teacher_type_index') }}">back to list</a>

    <a href="{{ path('teacher_type_edit', {'id': teacher_type.id}) }}">edit</a>

    {{ include('teacher_type/_delete_form.html.twig') }}
{% endblock %}
